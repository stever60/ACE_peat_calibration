Element: Zr

All calibration responses are log-transformed.
RMSE & RMSEP computed in log-space.
Predictions stored in *_Pred_ppm, *_L95_ppm, *_U95_ppm.

Model types:
  OLS, WLS, OLS(wt), WLS(wt), Bayes, RF, PLS(LOO), PLS(k).

Ranking:
  Highest R² → lowest RMSEP → lowest RMSE.
